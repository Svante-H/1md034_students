/*
const vm = new Vue({
    el: '#selectBurger',
    data: {
        bNo1: burger1.getName() + ': ' + burger1.getCalories(),
        bNo2: burger2.getName() + ': ' + burger2.getCalories(),
        bNo3: burger3.getName() + ': ' + burger3.getCalories(),
        bNo4: burger4.getName() + ': ' + burger4.getCalories(),
        bNo5: burger5.getName() + ': ' + burger5.getCalories()
    })

const vm = new Vue({
    el: "#selectBurger",
    data: {
        menu: menu
    }

})
*/
